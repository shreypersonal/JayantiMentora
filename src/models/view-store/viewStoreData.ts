import { ToastPreset } from '@constants'

const ViewStoreData = {
  apiStatusStore: {},
  toastStore: {
    message: '',
    preset: ToastPreset.Success,
    showToast: false,
  },
}

export { ViewStoreData }
